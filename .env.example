# Postgres connection string used by netlify/functions
DATABASE_URL=postgres://postgres:devpass@localhost:5433/streamclone

# Secret used to sign auth session JWTs — set a long random value in production
JWT_SECRET=dev-only-change-me
